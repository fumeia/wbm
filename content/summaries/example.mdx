import VisualizationFrame from '../../components/VisualizationFrame';
import { MathJax } from 'better-react-mathjax';

### The Harmonic Oscillator

The equation of motion for a simple harmonic oscillator is given by:

$$m \frac{d^2x}{dt^2} + kx = 0$$

This can be rewritten as:

$$\frac{d^2x}{dt^2} + \omega_0^2 x = 0$$

where $\omega_0 = \sqrt{k/m}$ is the natural frequency of the oscillator.

Below is an example of an embedded interactive visualization.

<VisualizationFrame />